{
  "dependencies": {
    "@nestjs/mapped-types": "*",
    "class-validator": "^0.14.1"
  },
  "devDependencies": {
    "husky": "^9.1.6"
  },
  "scripts": {
    "lint:backend": "cd backend && npx lint-staged --allow-empty",
    "prepare": "husky"
  }
}
